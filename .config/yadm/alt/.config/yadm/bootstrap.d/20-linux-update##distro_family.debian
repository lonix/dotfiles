#!/bin/bash

echo "Linux or WSL Detected...."
echo "Updating apt packages..."

sudo apt-get update
sudo apt-get dist-upgrade -y --force-confold

ehcho "Installing Necessary Packages"
sudo apt-get install -y git curl wget
